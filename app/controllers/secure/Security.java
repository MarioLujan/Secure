/*
package controllers.secure;

import models.Usuario;
*/

/*
 *
 * @author Mario Lujan
 */
/*

public class Security extends SecurityManager {
    
    static boolean autenticar(String nombre, String password) {
        Usuario usuario = Usuario.find("byEmail", nombre).first();
        if ("administrator".equals(usuario.administrador)) {
            return usuario.administrador;
        }
        else {
            return false;
        }
        //return usuario != null && usuario.password.equals(password);
    }
} 
*/